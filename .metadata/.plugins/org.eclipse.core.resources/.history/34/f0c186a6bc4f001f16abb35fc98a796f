package com.aurionpro.creational.prototype.model;

public interface IShape {
	public interface Shape {      // every shape should be able to clone itself and draw.
		Shape clone();           // Make a copy of itself
		void draw();             // Draw the shape
	}

}
